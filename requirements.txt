# Core dependencies (required for all scripts)
requests>=2.31.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0  # For configuration management

# Visualization and output formatting
rich>=13.0.0  # For pretty printing

# Note: The base scripts (01-04) run completely locally without ML dependencies
# For advanced features, see requirements-ml.txt and requirements-dev.txt
